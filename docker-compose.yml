services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: minto
      POSTGRES_PASSWORD: minto
      POSTGRES_DB: minto
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
